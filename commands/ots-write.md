---
description: Write a temporal node through the schema-enforced helper
---

Run the `ots-write` skill. The model proposes summary and saliency prose. `scripts/ots_common.py` commits frontmatter. Children before parent aggregates. Never invent `rel` values. Never write types this plugin does not own.
