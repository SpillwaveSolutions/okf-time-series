---
description: Tail host JSONL into an immutable .source.jsonl snapshot
---

Run the `ots-tail` skill (`once` / `start` / `stop` / `status` / `check` / `setup`). Read-only on the host file. No LLM. No stored emit schema. See docs/PRD-TELEMETRY-PHASE1.md.
