---
description: Tail host JSONL into an immutable .source.jsonl snapshot
---

Run the `ots-tail` skill (`once` / `start` / `stop` / `status` / `check` / `setup` / `opt-in` / `opt-out` / `smoke`). Capture is opt-in (`.okf-history` or `opt-in`). Install is not capture. Idle default 300s. See docs/PRD-TELEMETRY-PHASE1.md.
