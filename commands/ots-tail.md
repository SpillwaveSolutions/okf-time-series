---
description: Tail host JSONL into an immutable .source.jsonl snapshot
---

Run the `ots-tail` skill (`once` / `start` / `stop` / `status` / `check` / `setup` / `opt-in` / `opt-out`). Capture is opt-in (`.okf-history` or `opt-in`). Install is not capture. See docs/PRD-TELEMETRY-PHASE1.md.
