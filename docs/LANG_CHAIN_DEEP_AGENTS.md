# LangChain Deep Agents — okf-time-series

Install via filesystem `skills=` / SkillsMiddleware pointing at this repository.

```python
from deepagents import create_deep_agent
agent = create_deep_agent(skills=["/path/to/okf-time-series/skills"])
```

Identity: `SECOND_BRAIN_IDENTITY`. Bundle: `SECOND_BRAIN_ROOT`.
